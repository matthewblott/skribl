<turbo-frame id="pagination">
  <% note_view_models.each do |note| %>
    <%= render partial: "notes/note", locals: { note: note } %>
  <% end %>

  <% if pagy.next %>
    <turbo-frame id="pagination" src="<%= user_notes_path(page: pagy.next) %>" loading="lazy">
      Loading more...
    </turbo-frame>
  <% end %>

</turbo-frame>
