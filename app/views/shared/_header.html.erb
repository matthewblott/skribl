<% if Current.user %>
  <header>
    <span>
      Signed in as <%= Current.user.email %>
    </span>

    <%= button_to "Sign out", 
        Current.session, 
        method: :delete %>
  </header>
<% end %>
