<% if Current.user %>
  <header>
    <div>
      <h1>Scribble</h1>
    </div>
    <div>
      <span>
        Signed in as <%= Current.user.email %>
      </span>

      <%= button_to "Sign out", 
          Current.session, 
          method: :delete %>
    </div>
  </header>
<% end %>
