<% flash.each do |key, message| %>
  <div
    data-controller="alert"
    class="flash hotwire-native-hidden <%= key %>"><%= message %></div>

    <%= hidden_field_tag 'toast', '',
      data: {
        controller: "bridge--toast",
        bridge_message: message
      }
    %>

<% end %>
