<%= hidden_field_tag :user_id, Current.user.id,
  data: {
    controller: "bridge--sign-in-success"
  } %>

<script>
  //setTimeout(() => {
    //window.location.href = "<%= @redirect_path %>";
  //}, 1500);
</script>
