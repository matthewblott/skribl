<div class="container">
  <h1>New Note</h1>

  <%= turbo_frame_tag "note_form" do %>
    <%= render "form", note: @note %>
  <% end %>

  <div class="back-link">
    <%= link_to "Back to notes", user_notes_path(Current.user), class: "link" %>
  </div>
</div>
