<div>
  <h1>New Note</h1>

  <%= turbo_frame_tag "note_form" do %>
    <%= render "form", note: @note %>
  <% end %>

  <div>
    <%= link_to "Back to notes", user_notes_path(Current.user) %>
  </div>
</div>
