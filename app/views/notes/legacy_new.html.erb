<% content_for :title, "New Note" %>

<style>
  legend {
    span {
      margin-left: 0.5rem;
    }
    margin-bottom: 0.25rem;
  }
  label {
    display: flex;
    flex-direction: column;
    align-items: flex-start;

    span {
      margin-left: 0.5rem;
    }
    input, textarea {
      margin-top: 0.5rem;
    }
    input[type="checkbox"] {
      cursor: pointer;
      appearance: none;
      width: 1.25rem;
      height: 1.25rem;
      margin: 0;
      margin-top: 0.4rem;
      margin-left: 0.5rem;
      border: 1px solid #999;
      border-radius: 0.25rem;
      background: white;
      cursor: pointer;
    }

    input[type="checkbox"]:checked {
      background: #333;
    }

    input[type="checkbox"]:checked::after {
      content: "";
      display: block;
      width: 0.35rem;
      height: 0.65rem;
      margin: 0.15rem 0 0 0.4rem;
      border: solid white;
      border-width: 0 2px 2px 0;
      transform: rotate(45deg);
    }

  }
  
  textarea {
    padding: 0.5rem 1rem;
    border: 0.05rem solid darkgray;
    width: 100%;
    border-radius: var(--border-radius-pill);
    cursor: pointer;
  }

</style>

<% content_for :header_controls do %>
  <div role="group">
    <a href="<%= user_legacy_notes_path %>" role="button">Notes</a>
    <button form="note_form" formaction="<%= user_notes_path %>">Save</button>
  </div>
<% end %>

<% if @note.errors.any? %>
  <div style="color: red">
    <h2><%= pluralize(note.errors.count, "error") %> prohibited this note from being saved:</h2>
    <ul>
      <% note.errors.each do |error| %>
        <li><%= error.full_message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<form method="post" id="note_form">
  <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">

  <input type="hidden" name="note[img]" value="0">
  <input type="hidden" name="note[id]" value="0">

  <fieldset>
    <label>
      <span>Content</span>
      <textarea name="note[content]" class="autosize" class="pill" placeholder="Milk, eggs, bread"><%= @note.content %></textarea>
    </label>
    <label>
      <span>Completed</span>
      <input type="hidden" name="note[image_saved]" value="0">
      <input type="checkbox" name="note[image_saved]" value="1" <%= @note.image_saved ? "checked" : "" %>>
    </label>
  </fieldset>
</form>
