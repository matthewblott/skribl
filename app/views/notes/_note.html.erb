<div id="<%= dom_id note %>" data-action="click->ui--items#select">
  <% if note.image_saved %>
    <%= image_tag "/uploads/#{CurrentNoteContext.user_id}/#{note.id}.png" %> 
  <% else %>
    <%= note.created_at.strftime("%Y-%m-%d %H:%M:%S") %>
  <% end %>
</div>

